# 自然言語・説明サービス

利用者の自然言語入力から出発地、目的地、時刻、安全優先度を構造化し、APIが返す確定済みの経路・指数・根拠を基に比較と危険説明を生成する。

LLMに道路状況、通行可否、指数を推測・決定させない。外部ツールへのアクセスはREST APIに限定し、応答には参照したデータ時刻と仮データの注記を含める。

`src/ai_assistant/`にはデモ用の条件抽出・根拠説明、`tests/`には根拠を越えない説明のテストを置く。将来のLLM接続設定は`config/`、入出力契約は`docs/`、AI向け運用規約は`AGENTS.md`で管理する。
